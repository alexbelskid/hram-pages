server {
    listen 80;
    listen [::]:80;
    server_name hram.84-247-169-232.sslip.io;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name hram.84-247-169-232.sslip.io;

    ssl_certificate /etc/letsencrypt/live/hram.84-247-169-232.sslip.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hram.84-247-169-232.sslip.io/privkey.pem;

    client_max_body_size 128k;

    location / {
        proxy_pass http://127.0.0.1:8787;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
    }
}
